const AccountModalList = ({ children }) => {
  return (
    <>
      {/* list */}
      <ul className='right-0 flex w-60 flex-col gap-2 text-sm text-gray-500'>{children}</ul>
    </>
  )
}

export default AccountModalList
